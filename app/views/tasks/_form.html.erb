<%= form_for @task do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this check_in from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>



  <%=  f.hidden_field :user_id, :value => current_user.id %>

  <div class="field">
    <%= f.label :task %><br />
    <%= f.text_field :task %>
  </div>
  <div class="field">
    <%= f.label :task_frequency, "Daily" %>
    <%= f.radio_button :task_frequency, "daily" %>
    <%= f.label :task_frequency, "Weekly" %>
    <%= f.radio_button :task_frequency, "weekly" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
